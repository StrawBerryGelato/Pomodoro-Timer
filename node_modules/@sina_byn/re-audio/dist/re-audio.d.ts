import { JSX } from 'react/jsx-runtime';
import { RefObject } from 'react';

declare const Audio: {
    ({ playlist, defaultMuted, defaultShuffle, defaultVolume, defaultPlaybackRate, defaultRepeat, defaultTrackIndex, startMargin, autoplayOnTrackChange, children, }: AudioProps): JSX.Element;
    displayName: string;
};
export { Audio }

declare type AudioAnalyser = {
    getFrequencyData: () => number[];
};

declare type AudioChildren = React.ReactNode | ((audioContext: AudioContext) => React.ReactNode);

declare type AudioContext = AudioState & {
    audioRef: React.RefObject<HTMLAudioElement | null>;
    currentTrack: AudioTrack | null;
    playlist: AudioTrack[];
    play: () => void;
    pause: () => void;
    togglePlay: () => void;
    toggleMuted: () => void;
    toggleShuffle: () => void;
    setVolume: (newVolume: number) => void;
    setRepeat: (repeat: RepeatMode) => void;
    setCurrentTime: (newCurrentTime: number) => void;
    forwardTrack: (step?: number) => void;
    rewindTrack: (step?: number) => void;
    nextTrack: () => void;
    prevTrack: () => void;
    playTrack: (trackIndex: number) => void;
    setPlaybackRate: (newPlaybackRate: number) => void;
};
export { AudioContext as AudioContext }

export declare type AudioProps = {
    playlist: AudioTrack[];
    defaultMuted?: boolean;
    defaultRepeat?: RepeatMode;
    defaultShuffle?: boolean;
    defaultVolume?: number;
    defaultPlaybackRate?: number;
    defaultTrackIndex?: number;
    startMargin?: number | boolean;
    autoplayOnTrackChange?: boolean;
    children: AudioChildren;
};

export declare type AudioState = {
    playing: boolean;
    loading: boolean;
    duration: number;
    timeLeft: number;
    currentTime: number;
    muted: boolean;
    shuffle: boolean;
    repeat: RepeatMode;
    volume: number;
    playbackRate: number;
    trackIndex: number;
};

export declare type AudioTrack = {
    id: string | number;
    src: string;
    type?: string;
    fallbacks?: Pick<AudioTrack, 'src' | 'type'>[];
} & Record<string, unknown>;

export declare const formatTime: (time: number) => string;

declare type PaletteConfig = {
    coverKey?: string;
    colorCount?: number;
    defaultPalette?: string[];
};

export declare type RepeatMode = 'track' | 'playlist';

export declare const useAudio: () => AudioContext;

export declare const useCoverPalette: (paletteConfig?: PaletteConfig) => readonly [string[], boolean];

export declare const useVisualizer: (dataPoints?: number) => RefObject<AudioAnalyser>;

export declare const useVisualizerChart: (dataPoints?: number) => readonly [number[], boolean];

export declare const useVisualizerFrame: (dataPoints?: number) => number[];

export { }
